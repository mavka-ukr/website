# Зовнішні модулі

```
привіт.м
старт.м
```

Вміст файлу `привіт.м`:

```мавка
дія привіт()
  друк("Привіт!")
кінець

дати привіт
```

Можливі способи взяття модуля `привіт` (файл `старт.м`):

```мавка
взяти модуль привіт

привіт.привіт()
```

```мавка
взяти модуль привіт як суперпривіт

суперпривіт.привіт()
```

```мавка
взяти модуль привіт [привіт]

привіт()
```

```мавка
взяти модуль привіт [привіт як суперпривіт]

суперпривіт()
```

```мавка
взяти модуль назва_підпапки.привіт

друк(привіт)
```

Щоб взяти модуль відносно поточного модуля треба поставити крапку перед шляхом:

```мавка
взяти модуль .привіт
```

```мавка
взяти модуль .назва_підпапки.привіт
```
